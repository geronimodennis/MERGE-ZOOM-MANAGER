# Documentation Images

This directory stores screenshots and visual references used by the documentation.

Current images:

- `setup-capture-window.png`: setup screen for selecting one or more Zoom session captures.
- `live-demo-session.png`: synthetic Zoom-like Gallery View session from the local live demo.
- `detection-debug-reference.png`: reference image for participant rectangle detection debug overlays.

Release images to refresh from a real or test Zoom setup:

- `merged-gallery-multi-session.png`
- `single-pin-window.png`
- `group-pin-window.png`
